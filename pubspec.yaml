name: descubre_mi_ciudad
description: "A new Flutter project."
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.2.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  http: ^1.2.0
  shared_preferences: ^2.2.3
  url_launcher: ^6.3.1
  cached_network_image: ^3.3.1
  flutter_rating_bar: ^4.0.1
  intl: ^0.20.2
  share_plus: ^10.1.0
  geolocator: ^12.0.0
  provider: ^6.1.2
  image_picker: ^1.1.2
  flutter_localizations:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true

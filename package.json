{
  "name": "descubremiciudad-root",
  "version": "1.0.0",
  "scripts": {
    "start": "cd backend && node index.js",
    "install-backend": "cd backend && npm install",
    "build": "cd backend && npm install"
  }
}
